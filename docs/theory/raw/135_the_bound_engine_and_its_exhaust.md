# RAW 135 — The Bound Engine and Its Exhaust

### *Mass, Radiation, and Gravity from One Rendering Loop · Why Light Is c and Matter Is Not · The Eddington-Regulated Substrate*

**Author:** Tom (the mechanism and every load-bearing reframe), Claude (articulation, literature grounding, the timescale-lock and runaway-cap synthesis, honest ledger)
**Date:** 2026-07-12 (a single thinking conversation, no code — the sequel to RAW 134's Addendum F, prompted by "why did Exp 138 freeze?")
**Status:** Theory synthesis / working notebook. Assembles a candidate mechanism from parts the repo already earned or hand-built separately, plus one new diagnosis (why the geometrogenesis experiments could not work). **Nothing here is simulated in this form.** The central sign-competition is plausible and self-consistent, not proven; §8 is the honest earned-vs-assembled-vs-unproven ledger and §9 the banked test.
**Prerequisites:** RAW 118 (consumption gravity; gravity/radiation-pressure as two regimes of one saturation curve), RAW 128 (store/move/radiate partition), RAW 130 ("it rotates because it consumes"), RAW 133 (semantic leak-recruitment), RAW 134 (the inside-out substrate; esp. Addendum A objects-as-tapped-bundles, B no-overlap/space-as-rendered-delay, E gravity-as-differential-branching, F the comoving correction); Exp 128 v11 (the tick-budget γ, earned Schwarzschild tangential proper time); Exp 138 (geometrogenesis: P0 equilibrium condensation PASS, P1/P1c directed-growth NEGATIVE).
**Relates to:** this is the mechanism RAW 134 Addendum F §18.2 asked for — "a growth rule with a genuine comoving (polynomial-in-time, self-critical) regime." It proposes that the missing ingredient was *radiation*. **Paired with RAW 136** (the manifold is an attractor): 135 = the *engine* (mass, light, gravity from one loop); 136 = the *selection principle* (why the growth-driven, radiation-dissipative substrate should land on a manifold — SOC + directed time). One substrate, one banked experiment (§9 here = P1d there).

---

## What this document is

A RAW — a working notebook (per `feedback_raw_doc_philosophy`), preserving the live reasoning of one conversation. It records a mechanism Tom assembled over ~five turns while diagnosing why the Exp 138 geometrogenesis substrate only ever froze, exploded, or fragmented. The value is not that any piece is new — most pieces are in the repo already — but that they fall out of *one* loop, and that the loop names precisely what the experiments left out. It includes §11 (Document History), §12 (Wrong Turns / open problems), and a prior-art section (§10).

The honest discipline held here (as in RAW 134): where a claim is already earned elsewhere, it is marked earned; where it is only assembled tonight, marked assembled; where it could fail in a real run, marked unproven. No emergence is claimed that isn't in code (`feedback_honest_emergence_claims`).

---

## Abstract

One rendering loop, followed to its consequences:

1. **A pattern holds by re-rendering itself on its own clock** — and that clock is *slower than c*, set by the pattern's extent, not by the substrate step. (This is the tick-budget γ of Exp 128 v11, restated: proper time = the pattern's internal loop length.)
2. **Holding necessarily sheds.** A self-maintaining loop cannot reincorporate everything; what falls out each cycle — depleted, spent deposits — must leave. This is not an add-on; it is Prigogine's theorem that dissipative structures export entropy ("entropy is the price of structure"). Holding *is* radiating.
3. **The exhaust streams at c, and that is what light is.** A deposit bound in the loop rides the pattern's slow clock; a deposit thrown clear of the loop has no clock of its own and reverts to the only rate left — the bare frontier rate, c. So *bound = slow (matter), unbound exhaust = fast (radiation)*, and the enormous speed gap between matter and light is this distinction, not a free parameter.
4. **The exhaust is the coupling between patterns**, and it does two opposite things at once: it **pushes** the receiver (radiation pressure, momentum — repulsive) and it is **consumed** by the receiver, feeding growth on the side it arrives from (the body drifts up-flux — attractive). Same stream, opposite signs.
5. **The crossover between them is the Eddington limit** — the point where outward radiation pressure (∝ flux) balances inward consumption-growth (∝ consumed flux). Below capacity, consumption wins: attraction, diluting as 1/r² over the frontier (the N(r) ∝ r² area-law exponent — the same exponent that makes space 3D; RAW 134 Addendum E). At/above capacity, the un-consumable excess pushes: radiation pressure. This is RAW 118's "one saturation curve, two regimes," now with a mechanism instead of an assertion.
6. **The two signs separate only for a slow pattern.** The push arrives every substrate-tick and, against a slowly-re-rendering body, averages to jitter; the consumption-drift is slow and directional and accumulates. Attraction beats the push *iff* the pattern's clock is sub-c. With no timescale separation the two never separate → shred or freeze. **The slow-clock (point 1) and the coupling (point 4) are one condition seen twice.**
7. **Consume→grow→consume-more is positive feedback = runaway** (literally the Exp 138 P1b explosion). The push at saturation is what caps it. Push and pull *together* self-regulate at the Eddington balance — and a growth rate pinned there is exactly the **comoving, polynomial-in-time, self-critical phase** RAW 134 Addendum F hunted and Exp 138 P1c failed to find. **It failed because the substrate had no exhaust at all** — the selector was pure removal (decay), no shed-and-reconsume channel, hence no regulator, hence only explosion or freeze.

The claim of the document: mass, light, the speed gap between them, the inter-mass coupling, the 1/r² law, gravity's weakness, and the reason the geometrogenesis experiments could not work — all fall out of "a bound loop sheds exhaust that pushes-and-feeds." Most parts pre-exist; the assembly and the retro-diagnosis are new.

---

## 1. The starting point: the freeze problem and the slow clock

RAW 134 Addendum F closed Exp 138 with a negative: directed forward growth (the framework's arrow) produced no geometric phase — only explosion, freeze, or fragmentation. The worry Tom raised: **the experiments ran the gravity-analog at c.** Growth is c (the frontier, causality, the sample-rate limit — RAW 134's founding axiom). But we also fired selection/decay at full strength *every tick* — i.e. we made gravity a c-rate operation. Real gravity is not: it is a whisper (coupling ~10⁻³⁹) that becomes visible only after enormous numbers of light-crossing times. Collapse that hierarchy to 1:1 and you get freeze (selection wins instantly) or blow-up (growth wins) — never the slow accretion between, *because the between is the timescale separation itself.*

Tom's resolution flipped the sign of the worry: **the fast substrate is not the enemy — it is what gives a slow pattern room to breathe.** Because expansion runs at c, a pattern gets many substrate-ticks to complete one of its own re-renderings. Its clock is set by its extent — how long its internal loop takes to come back around — not by c. A big pattern re-renders slowly against a fast background; the gravitational update happens on the *pattern's* clock, which is intrinsically sub-c. The experiments froze because their "patterns" were one tick old — born and judged in the same step — so their clock *equalled* c. No slack, no proper time, instant lock.

This is not a new mechanism to invent. It is the **tick-budget γ earned in Exp 128 v11**: a pattern's clock runs slower the longer its internal path. "The pattern re-renders in its own clock speed" *is* proper time. Gravity being sub-c falls straight out of it. (Earned.)

And the other half of Tom's image — **"the time deposits are still embedded in the final visible 3D structure"** — is the fossil record: the pattern re-renders by reading the deposits its previous renderings laid down, and the visible 3D structure *is* that trail. Space is the pattern's rendering of itself, crystallized. This is Doc 49 ("space from time-differences") and RAW 134 §13.2b (object-is-an-observer) restated: the past does not re-thermalize (the immutable-past axiom forbids it); it grows, and the pattern re-projects itself into the fresh substrate each proper-tick.

## 2. Holding necessarily sheds (and why at c)

The question this raised: last, we had split the problem into "how does a pattern hold" (call it solved-enough — the self-maintenance line of Exp 136/138, cycle closure, Doc 28 renewal) and "how does one pattern reach another." Tom's next move fused them: **a self-closing loop necessarily sheds what it cannot reincorporate.**

This is **Prigogine's dissipative-structures theorem** (Nobel 1977): a structure maintained far from equilibrium *must* export entropy to persist — "entropy is the price of structure" (openness + far-from-equilibrium + nonlinearity + **dissipation** are the four required conditions). A pattern that holds by re-rendering, each cycle, has deposits it can't fold back into the active loop; since the past is frozen and the frontier moves at c, they are carried off. **Holding ⇒ shedding.** The coupling channel is therefore not a second mechanism to build — it is the *guaranteed exhaust* of the first. (Earned in the general thermodynamic literature; newly *identified* with the framework's shed-deposit stream here.)

Why at c specifically — a framework-native answer: a deposit *in* the loop is bound to the pattern's slow clock; a deposit thrown *out* has no loop, no clock of its own, so it reverts to the bare frontier rate, c. **Bound = slow, unbound = c.** This is the cleanest account the project has of why light is fast and matter is slow: they are the same deposits, one still in an engine, one thrown clear of it. (Assembled.)

## 3. The shed stream is the coupling — radiation

The exhaust streams outward at c and reaches other patterns. When it does, it enters their re-render — coupling the two. This lands exactly on the three-state split the repo already carried (V3 ch002; RAW 118): **Same** = reincorporated (the loop, mass), **Different** = shed (radiation), **Unknown** = frontier (expansion). Tom re-derived the radiation leg from the *maintenance* side (shedding) rather than positing it. (The three-state split is earned/prior; the derivation-from-holding is new.)

So: what a pattern *is* (a bound re-rendering loop = mass), what light *is* (the loop's unbound exhaust), and *how mass reaches mass* (one irradiates the other's re-render) are three faces of one loop.

## 4. Push and pull: opposite signs from one stream

The hard fork — does the coupling attract (gravity) or repel (radiation pressure)? Tom's answer: **both**, and they point opposite ways, which is the engine, not a fudge.

- **Push (repulsive):** absorbed radiation carries momentum; it shoves the receiver away from the source. Radiation pressure.
- **Consume-and-grow (attractive):** the receiver consumes the arriving deposits and grows on the side they arrive from — its body drifts *up-flux*, toward the source. This is precisely RAW 130's consumption-as-centripetal ("it rotates because it consumes," F = −consumed/r²) and RAW 133's leak-recruitment (a neighbor's deposits bias your re-render toward it). (Earned internally.)

The attraction goes as consumed flux, and the flux **dilutes as 1/r²** because the shed deposits spread over the expanding frontier, whose shell count grows as N(r) ∝ r² — *the same exponent that makes rendered space 3D* (RAW 134 Addendum E: Newton's tail and 3D-ness are one exponent). So **attraction ∝ 1/r², Newton**, with the exponent sourced from the area law rather than assumed. (The 1/r²-from-N(r)∝r² is earned in Addendum E; its appearance *here as the radiation dilution* is the new tie.)

## 5. The crossover is the Eddington limit

Which effect wins is set by how much the receiver can consume:

- **Below capacity:** consumption dominates → net attraction (gravity), ∝ 1/r².
- **At/above capacity:** the excess flux it *cannot* consume just pushes → net repulsion (radiation pressure).

The balance point — outward radiation pressure (∝ luminosity/flux) equal to inward consumption-pull (∝ consumed flux, tracking enclosed mass) — is the **Eddington limit**, and in mainstream astrophysics it is a *self-regulator*: the same photon-momentum mechanism that pulls (via the mass it traces) pushes (via the flux), and their equality caps accretion luminosity (Eddington-limited accretion onto stars and black holes). RAW 118 already named "gravity and radiation-pressure as two regimes of one saturation curve, Eddington = receptor saturation threshold" — this document supplies the mechanism under that assertion: the two regimes are consume-wins vs push-wins on one shed stream. (The Eddington balance and its self-regulation are earned/mainstream; the mechanism-under-RAW-118 is the contribution.)

A bonus this hands us: **why gravity is weak.** Only the small fraction of the shed flux that gets *re-consumed as binding* pulls; the rest streams away as light. The gravitational coupling is the tiny absorption-into-binding cross-section of a mostly-escaping radiative flux. A candidate account of the 10⁻³⁹, banked not claimed. (Assembled, speculative.)

## 6. The timescale lock: attraction beats the push only for a slow pattern

The push and the pull are not simply opposed vectors — they act at different *rates*. The push is a fast, c-channel kick: it arrives every substrate-tick and, against a slowly-re-rendering body, **averages to jitter**. The consumption-growth is slow and **directional**: it accumulates as a steady drift over many pattern-cycles. So the attraction survives the competition *iff* the pattern's clock is slow enough for the fast push to wash out while the slow drift accumulates.

This locks §1 and §4 into one requirement: **the sub-c pattern clock is exactly what separates the repulsive push (fast, averages to noise) from the attractive growth (slow, accumulates).** With no timescale separation — the frozen one-tick "patterns" of Exp 138 — push and pull are both per-tick, never separate, and you get the shred-or-freeze mush. The proper-clock reframe and the radiation-coupling are the same condition seen twice. (New synthesis.)

## 7. Runaway → cap → the comoving phase (retro-diagnosis of Exp 138)

Consume-makes-you-grow-makes-you-consume-more is **positive feedback**, and positive feedback is *literally the Exp 138 P1b explosion* (supercritical growth, no stable size). What caps it? The push. At saturation the excess flux shoves back and throttles growth. So push and pull *together* self-regulate at the Eddington balance — and a growth rate pinned at that balance is precisely the **comoving, self-critical, polynomial-in-time phase** RAW 134 Addendum F §18.2 specified as the true target ("the geometric phase grows, hidden by scale; it is not absolute stationarity").

Therefore the sharpest consequence of the whole thread: **Exp 138 could not have found the geometric phase, because its substrate had no exhaust at all.** The selector was pure removal — decay with no shed-and-reconsume channel — so there was nothing to cap the runaway and nothing to feed directional growth. No radiation, no regulator, no comoving phase; only explosion (growth wins) or freeze (decay wins). This is a *diagnosis with a fix*, not a consolation: the missing ingredient is a concrete, buildable thing — the shed → dilute-as-1/r² → reconsume loop — and it predicts the geometric phase should appear at the push-pull balance once that loop exists. (New; this is the document's operational payoff.)

## 8. Earned vs assembled vs unproven (the honest ledger)

- **Earned (in code or mainstream), reused here:** the tick-budget γ / proper time (Exp 128 v11); consumption-as-centripetal, F=−consumed/r² (RAW 130, Exp 128 ODE); 1/r² from N(r)∝r² (RAW 134 Addendum E); the Eddington balance and its self-regulation (mainstream astrophysics); dissipative structures must export entropy (Prigogine); the three-state Same/Different/Unknown split (V3 ch002, RAW 118).
- **Assembled tonight (new joins, not new physics):** holding ⇒ shedding ⇒ the coupling channel is the guaranteed exhaust; bound=slow / unbound=c as the matter/light speed gap; push-and-consume as the two signs of one stream; the timescale-lock (slow clock is what lets attraction beat the push); gravity's weakness as the small binding-cross-section of an escaping flux.
- **Unproven / could break in a real run:** that consume-and-grow actually *beats* the push in the sub-saturation regime (a simulation could find the push wins everywhere — the naive LeSage/radiation-pressure failure, RAW 134 Addendum E located it); the momentum bookkeeping (where the push's momentum goes — RAW 128 would "store it ahead" of the pattern; unverified that this closes); that the Eddington-balanced growth is actually *polynomial* in time and *geometric* in shape (the comoving claim), not merely bounded; whether any of this survives the RAW 134 §5 wall (an emergent-geometry instrument that fails its own random-graph null — Exp 138 P1c — cannot certify the resulting phase regardless).

## 9. Falsifiable claims / banked experiment

The claim is not "gravity is tunable" but structural:

1. **Minimum extent.** There is a pattern extent below which everything freezes (the Exp 138 regime) and above which gravity-rate falls as ~1/extent (the proper clock). Below the threshold, no attraction; above it, weak-and-slow attraction.
2. **The exhaust experiment (the real P1d).** Give the substrate a shed → 1/r²-dilute → reconsume channel with momentum (push) *and* consumption-growth (pull) on the same stream, patterns with genuine extent (a slow clock, not one-tick births), and sweep the flux/capacity ratio. Pre-registered outcomes: **(a)** at low ratio, attraction with a growth rate self-pinned near the Eddington balance → comoving polynomial-in-time growth with stationary shape (the geometric phase); **(b)** at high ratio, radiation-pressure blow-apart; **(c)** if push wins at *all* ratios → the mechanism is refuted (sign-competition fails), a clean negative. Reachable-range rule (per the Exp 137/138 lessons): confirm both PASS and FAIL cells are attainable before freezing; and — the Exp 138 P1c lesson — **validate the geometry instrument against a random-graph null first**, or no PASS is certifiable.
3. **Eddington ratio as an order parameter.** If the framework is right, the sub/super-critical boundary of structure formation should track the push-pull (Eddington) ratio, not a bare growth rate — a scan should show the comoving band centered on the balance.

## 10. Prior Art and Connections

- **Prigogine, dissipative structures (Nobel 1977):** self-organization far from equilibrium *requires* entropy export — the theorem under "holding necessarily sheds." ("Entropy is the price of structure.")
- **Eddington luminosity / Eddington-limited accretion (mainstream astrophysics):** radiation pressure (∝ L) vs gravity (∝ M), same photon-momentum mechanism, self-regulating the accretion rate. The push-pull crossover is this, and its self-regulation is the candidate comoving-phase stabilizer.
- **LeSage / shadow gravity; radiation pressure — the cautionary twin.** Naive "absorb radiation, get pushed" gives the *wrong sign* (repulsion) and the classic heating problem (RAW 134 Addendum E located this). This document's escape is that the stream *also feeds consumption-growth* (attraction, up-flux), and the timescale-lock lets the slow pull beat the fast push — but if a simulation shows the push dominating, we are back in LeSage's grave. Named so it can't hide (§8, §12).
- **Internal:** RAW 118 (Eddington-as-saturation, the two-regimes assertion this puts a mechanism under), RAW 130 (consumption-centripetal, the attractive channel), RAW 128 (store/move/radiate — the shed leg and the momentum-storage bookkeeping), RAW 133 (leak-recruitment, drift-up-flux), RAW 134 Addenda A/B/E/F (objects-as-observers, space-as-rendered-delay, gravity-as-differential-branching with 1/r²⟺N(r)∝r², the comoving correction), Exp 128 v11 (γ tick-budget), Exp 138 (the negative this diagnoses).
- **Whitehead, *Process and Reality* (via RAW 134 §13.3):** the pattern (a *society* of occasions) endures by re-enacting itself and prehending others — the re-render-and-couple structure, computationalized. Whitehead's societies also *perish* into "objective immortality" (become data for successors) — the "time deposits embedded in the 3D structure" almost verbatim.

## 11. Document History

- **2026-07-12, this conversation (no code):**
  - Tom: the Exp 138 freeze is because we ran gravity at c; fear that emulating our (slow) gravity at c-speeds must freeze. → Located as a timescale-separation problem.
  - Tom (the flip): the fast substrate *gives the slow pattern room*; the pattern re-renders on its own (sub-c) clock; the time-deposits stay embedded in the 3D structure. → Identified with the earned γ tick-budget (Exp 128 v11) and Doc 49 space-from-time.
  - Tom: holding (solved-enough) vs coupling (open); coupling candidate = radiation = depleted processes shed from the render cycle, moved at c. → Fused via Prigogine (holding ⇒ shedding); "why at c" answered (unbound = no clock = frontier rate).
  - Tom (the fork resolved): it pushes *and* is consumed, and consumption allows faster growth. → Push/pull opposite signs; crossover = Eddington (mainstream self-regulator); timescale-lock (slow clock separates the signs); runaway-cap → the comoving phase; retro-diagnosis: Exp 138 froze because it had no exhaust.
  - Claude: literature grounding (Eddington self-regulation; Prigogine entropy-export), the earned/assembled/unproven ledger, the banked exhaust-experiment.

## 12. Wrong Turns and Open Problems

- **12.1 The sign is not guaranteed — the LeSage grave is open.** The entire mechanism rests on consume-and-grow beating the push in the sub-saturation regime. If it doesn't (a real run shows radiation pressure dominating at all separations), the coupling is repulsive and the picture is dead. This is the single most likely failure mode and it has a distinguished graveyard (LeSage). §9 test (c) is the kill switch.
- **12.2 Momentum bookkeeping unclosed.** The push delivers momentum; conservation demands it be accounted (RAW 128 stores momentum "ahead" of the entity). Whether the shed/absorbed momentum balances across a two-pattern system without a hidden sink is unverified.
- **12.3 "Comoving = geometric" is still a leap.** Even a self-regulated polynomial-in-time growth need not be *geometrically* 3D; and the RAW 134 §5 wall plus the Exp 138 P1c instrument-invalidity finding mean we cannot certify geometry until a dimension instrument survives a random-graph null. Self-regulation buys "not frozen, not exploded" — not yet "manifold."
- **12.4 Extent vs the immutable past.** A pattern must *hold its extent* while the substrate expands under it (what keeps the loop closed as everything grows). This is the half Tom flagged as "already tried in previous geometric substrate" (Exp 136/138) — unsolved there; inherited here as a prerequisite, not a result.

---

## 13. Addendum (2026-07-12): the pull half, dynamically demonstrated on the flooding-lag substrate

A minimal dynamic model (`experiments/138_geometrogenesis/flooding_lag_engine.py`): deposits flood a pipe network (scalar flux diffusing along edges, uniform ambient emission), and one node is a **consumer** that removes the flux it reads (perceiving = consuming, §4). Result: the consumer carves a **well** whose deficit vs graph distance is the **graph Green's function of the local dimension** — **log(r) on a 2D lattice (R²=1.000), 1/r on a 3D lattice (R²=0.999)**. So a consuming observer produces the gravitational potential of the correct dimension — the 1/r potential gives 1/r² force (Newton) — **no field, no coordinates, just the graph + flooding + a sink.** This dynamically confirms the *depletion* channel of §4–5 and Addendum E (1/r² ⟺ dimension), and unifies with RAW 137: the same consumer both perceives (reads lag) and gravitates (carves the well), and the well *carries the local geometry* (log vs 1/r).

**Honest bound:** this is the **pull half only.** A sink producing its Green's-function well is near-guaranteed by diffusion theory; what is shown is that *if* the observer consumes, the resulting well is attractive and dimension-correct on this substrate. It does **not** test §12.1's kill switch — whether the **push** (radiation pressure) stays subdominant so the *net* sign is attractive — because the minimal model has no push. The sign competition (§9 test (c)) remains the open question; this result establishes only that the depletion side, alone, behaves as claimed.

### 13.2 The push half — kill switch pushed on (2026-07-12); §6 refuted, the sign reduced to one unresolved question

A minimal radial model with BOTH channels (`push_pull_renewal.py`): push = momentum (absorbed outward flux adds outward velocity, ∝Φ=1/r²); pull tried two ways; renewal (Doc 28) resets accumulated velocity every W ticks. Findings:

- **§6 is refuted.** §6 claimed the push "averages to jitter" against a slow-rendering pattern. It does not — a **steady radiation force accumulates** (solar sails). The averaging story is dead; delete it as a resolution.
- **Renewal-reset does not rescue attraction** (with pull=accretion). Fast renewal (small W) gives at best *bound* (holds station, never falls in); slow/no renewal gives *escape* (LeSage). No infall at any W; even 8× pull barely dents r. The renewal-reset idea I floated is not a resolution.
- **The kill switch reduces to one question the framework answers two ways.** Is consumption a **force** or an **accretion**? (i) pull = **accretion** (a position shift toward the source — RAW 135 §4's own wording): the steady push generically wins → **LeSage's grave** (escape across β/α up to 4× in the fork test). (ii) pull = **force** (velocity change — what Exp 128 v11, the *earned* 1/r²+Kepler result, implicitly used): net force ≈ (β−α)/r², so **infall iff β>α** (the Eddington condition), renewal irrelevant to the sign (fork test: infall at β/α=4). **Gravity survives IFF the pull is a force and β>α** — and §4 (accretion) contradicts Exp 128 (force) on exactly the point that decides gravity-vs-LeSage.

Net: the kill switch is **real, sharp, and unresolved**, now reduced to *is consumption a force or an accretion*, with the two in-house answers giving opposite verdicts. This corrects the RAW (§6 dead; §4 in tension with Exp 128) rather than confirming the mechanism — the honest outcome of pushing on a kill switch.

### 13.3 Kill switch SETTLED by fixing the point of view (2026-07-13); §4 corrected

The fork was a **god-view artifact** (Tom: "previous models/RAWs may assume something wrong because of wrong PoV"). §4 assumed the pull comes from the pattern **consuming the star's own flux and growing toward it** (accretion) — but a deposit from the star travels *outward*, so consuming it delivers *outward* momentum and pushes you *away*. That mis-PoV is exactly why the accretion model kept losing to the push: "eat the star's light → move toward the star" has the sign backwards.

**Correct PoV — gravity is a FORCE, the star's shadow in the ambient vacuum bath.** A mass has two effects on the one ambient field (`two_flux_gravity.py`): **emission** L → a peak → test objects pushed down-gradient, *away* (radiation pressure); **ambient consumption** s → a well (shadow) → test objects pushed down-gradient, *toward* the mass (gravity). Both are one kind of thing — a flux-pressure gradient, a **force** (−∇F). Demonstrated crossover (torus3d, force at r=2): pure consumer +0.30 (attract) → star s=0.5,L=1 +0.25 → L=3.3 +0.15 → super-Eddington L=6 +0.03 → **pure emitter −0.58 (repel)**. Consumption attracts, emission repels, and emission weakens then flips the net — the **Eddington crossover**, with **no accretion channel and no consuming-the-star's-light**.

- **Force-vs-accretion resolved:** it is a **force**. "Accretion" was the god-view mis-name of the shadow-gradient force.
- **Kill switch (sign) settled:** gravity survives — an attractive flux-gradient force from the ambient shadow, winning generically because a mass consumes ambient renewal far more than it emits (Exp 128 v11: renewal is the local ambient tick-stream, stellar flux only surplus → s≫L in force terms), flipping to radiation pressure only super-Eddington. Consistent with the earned Exp 128 + thread 2's well + Addendum E's depletion-side sign.

**Honest new bound — this is LeSage done right, and inherits LeSage's *other* problems.** Gravity-as-ambient-shadow is pushing/shadow gravity (Addendum E located it), so it inherits the classical LeSage difficulties the sign fix does not touch: (1) **drag** — a *moving* body should sweep more flux on its leading face and decelerate (orbits decay); (2) **heating** — absorbed flux should cook bodies. Candidate framework-specific escape: **renewal** (Doc 28) — tested next.

### 13.4 Renewal DEFEATS the LeSage drag (2026-07-13) — the oldest axiom pays a debt

Tom's resolution of the drag problem, and it holds: **in this framework entities do not persist-and-move; they renew.** LeSage drag is aberration — a body *moving through the bath* sees forward-tilted flux that saps its momentum — and aberration is proportional to velocity **relative to the bath at the moment the flux is read.** A renewing pattern (Doc 28) forms fresh from its local bath each cycle, so at the moment it reads it is **at rest in its own renewed frame**; drag can only grip the velocity acquired *since* the last renewal (∝ acceleration × W), never the full orbital velocity, which is baked into the renewed rest-frame. *"Entities don't move; their perception through the pipeline moves"* — and a rendering-shift has nothing for drag to grip.

Tested (`orbit_drag_renewal.py`): a 2D orbit under central gravity + LeSage drag F = −k·(v − v_frame), with the drag rest-frame v_frame re-based to the body's current velocity every W ticks (renewal); v_frame = 0 fixed = the classical static-bath body. Result (k=0.02): **persistent body (never renews) DECAYS — crashes from r₀ to 0.05 r₀** (the classical drag death); **fast renewal (W≤10) stays STABLE** (r_final 0.97–0.999 r₀); transition around W≈50. Across drag strengths a persistent body always crashes (even k=0.01 → 0.05 r₀) while a W=1 body survives 10× the drag (k=0.2 → 0.98 r₀). Renewal re-bases the frame — it never touches position or orbital velocity directly — so this is non-question-begging: it demonstrates that *drag measured against a renewed frame cannot see the bulk velocity.*

**So the framework's gravity clears BOTH classical LeSage graves:** the **sign** (§13.3, the ambient-shadow PoV fix) and the **drag** (§13.4, renewal). And the axiom that does it — **renewal / temporal surfing (Doc 28, RAW 028)** — was posited years ago for *identity* reasons (patterns persist through renewal, not translation); it turns out to be exactly what makes shadow-gravity viable. Still open (banked): **heating** (does the ambient consumption cook bodies, or is what's consumed the already-spent/depleted flux? — §4's "depleted deposits" framing is the candidate answer, untested), and porting this from the toy orbital ODE onto the actual flooding-lag graph substrate.

---

*Status: synthesis draft. One rendering loop — hold, shed, push-and-feed — reproduces mass, light, their speed gap, the inter-mass coupling, 1/r², and gravity's weakness, and diagnoses why the geometrogenesis experiments (no exhaust → no regulator) could only freeze or explode. Most parts pre-existed; the assembly and the retro-diagnosis are the contribution. The pull half is now dynamically demonstrated (§13): a consuming node on the flooding-lag graph carves the dimension-correct gravitational well (log 2D / 1/r 3D), no coordinates. The attractive NET sign is still not proven — the push half is untested (§12.1 is the kill switch). The banked exhaust-experiment (§9) is the next buildable step, gated behind a random-graph-validated geometry instrument (Exp 138 P1c lesson).*
